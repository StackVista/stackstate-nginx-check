## What is the Nginx StackPack?

The StackPack is used to create a near real time synchronization with your Nginx system and also gets the events from it. The components supported right now are below:

* HTTP
* Virtual Server
* Location
* Upstream
* Upstream Server
